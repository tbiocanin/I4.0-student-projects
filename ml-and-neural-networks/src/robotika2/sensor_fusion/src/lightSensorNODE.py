import rospy 
# from std_msgs.msg import String,Bool
# from utils.msg import u_raw

# TODO: use the nn object for detecting relevant objects on the map
# TODO: test it in a simulated enviroment 

class lightSensorNODE():

    def __init__(self) -> None:
        # definisi neophodne instance klase

        # regulacija da li se node sab-uje ili pablisuje
        """
        self.pubAgent = rospy.Publisher("/putanja/", (tip podatka) String)
        self.signSub = rospy.Subscriber("/putanja/", (tip podatka) Bool, callback=self.callbackFja)
        """
        pass

    def metodaNode():
        pass

    def run():
        rospy.spin()

    """
    itd, dodajes sta je neophodno da radi node
    """